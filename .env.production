# next build/next start(배포 빌드)에서만 로드된다 - next dev(로컬 개발)는 .env.local의
# localhost:8080을 그대로 쓰고 이 파일을 읽지 않는다. 백엔드(EC2) IP가 고정되어 여기 커밋한다 -
# 시크릿이 아니라 공개 API 엔드포인트 주소이므로 .env.local과 달리 git에 포함해도 안전하다.
NEXT_PUBLIC_API_BASE_URL=https://54.116.65.225
